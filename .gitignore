# byte-compiled / caches
__pycache__/
*.py[cod]

# runtime logs & captures (not source)
logs/

# raw session working files (provenance only, disposable)
session_scratch/

# private keys / credentials — never commit
keys/
*.pem
CONNECTIONS.txt

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/
