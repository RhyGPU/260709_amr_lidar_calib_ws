# 2026-07-09 (수)

> Session spanned 2026-07-08 evening → 2026-07-09. AMR (SeerRobotics `Foil_A082`)
> 2D LiDAR access: from "is it even reachable" to a packaged toolset + a fixed
> calibration app. Data path settled on the **SEER API** (open, no credentials).

## 작업 목록
- [x] USB-C 이더넷으로 AMR 연결 확인 + 서브넷 정정
- [x] SEER API(19204) 접속 → LiDAR/상태 조회
- [x] Seer-API.pdf 스펙 확보(.eml 첨부 추출)
- [x] 2D LiDAR(.100/.101) 탐지 + CoLa2 세션 확인
- [x] RViz 스타일 스냅샷 시각화(artifact)
- [x] RoboShop Pro + WiFi 경로(192.168.44.82) 확인
- [x] SEER API 실시간 뷰어 작성(`amr_lidar.viewer`)
- [x] UDP 발행 릴레이(인증) 서버/클라이언트 작성
- [x] 캘리브레이션 앱용 SICK MS3 UDP 에미터 작성
- [x] 워크스페이스 패키지화(`amr_lidar_ws`)
- [x] 캘리브레이션 앱 사본에 auto-fit + range cap 수정
- [x] 문서화(worklog / issues_and_fixes / code_updates)

## 상세

### 1. AMR 네트워크 도달 + 서브넷 정정
- **배경**: USB-C 이더넷은 물리 연결됐으나 데이터가 안 들어옴.
- **변경**: 어댑터가 link-local(169.254.x)로 자가할당된 상태였음. pktmon 캡처로 로봇측이 `192.168.192.0/24`임을 확인(로봇이 `192.168.192.13`을 ARP 요청). 어댑터에 정적 IP `192.168.192.13/24` 부여(`netsh interface ipv4 add address name=38 ...`). 컨트롤러 `192.168.192.5` 확인. ✓(live ping/ARP)
- **결정**: 로봇이 이미 찾고 있던 `.13`을 사용.

### 2. SEER API 접속 + LiDAR 조회
- **배경**: 로봇 LiDAR 상태 확인 필요.
- **변경**: SEER TCP/JSON 프로토콜(16B 헤더 `5A 01 …` + JSON) 클라이언트 작성. 상태 API(19204) msg **1009**(laser)로 `FrontLiDAR`/`RearLiDAR` 조회. 모델 `Foil_A082`, fw `v3.4.5.22`. ✓(Seer-API.pdf §15.1.1.3.9 / live)
- **결정**: LiDAR 접근은 SEER API 경로로 확정(개방 포트, 인증 불필요).

### 3. API 스펙 확보
- **변경**: Downloads의 `.eml`(→.JSON로 개명돼 있던) 이메일에서 `Seer-API.pdf`(428p) 첨부를 추출 → `docs/seer-api.pdf`로 보존. 포트/헤더/1009·1004·1300·1301 등 확인. ✓(PDF 직접 인용)

### 4. 2D LiDAR 탐지 + CoLa2
- **배경**: "2D LiDAR 연결 감지/포착".
- **변경**: `.100`(front)/`.101`(rear) ping alive, CoLa2 TCP **2122** open, `CreateSession` 핸드셰이크 성공(세션 확립). UDP `MS3 ` 스트림은 로봇 PC(.5)로만 unicast되어 2번째 PC엔 안 옴 확인.
- **결정**: 사용자 정정 — 이 센서는 **4× SICK TiM320-0131000S02**(safety+mapping+nav 겸용). CoLa2/UDP 우회 대신 SEER API 경로 사용.

### 5. 시각화 & 대체 경로
- **변경**: RViz 스타일 스냅샷 HTML artifact(`artifacts/amr_scan_view.html`) 생성(pose+nav LiDAR+stations). RoboShop Pro(`RoboshopPro24176.exe`)가 WiFi `192.168.44.82`로 접속 → 동일 로봇 확인. ✓(live: 동일 id/pose)

### 6. 라이브 뷰어 + UDP 릴레이 + MS3 에미터 (패키지)
- **변경**: `amr_lidar/` 패키지 작성 — `seer_client`(공유), `viewer`, `relay_server`/`relay_client`(id/pw 인증 UDP 팬아웃), `ms3_emitter`(SEER→SICK MS3 재발행). 릴레이/뷰어/에미터 모두 live 검증.
- **결정**: 캘리브레이션 앱은 ip:port MS3 리스너이므로 **MS3 에미터**가 정답(앱 무수정, 인증 불필요). 에미터는 앱 자체 NanoScan3Receiver로 왕복 검증(520pts/~450 valid).

### 7. 워크스페이스 정리
- **변경**: 흩어진 스크립트를 `amr_lidar_ws/` 패키지 구조로 통합(scripts/ docs/ tools/ artifacts/ keys/ logs/ + pyproject/.gitignore). 중복 SEER 클라이언트를 `seer_client.py`로 단일화.

### 8. 캘리브레이션 앱 수정(사본)
- **배경**: jog/각도 변경 시 LiDAR가 화면에서 영구적으로 사라짐(되돌려도 복구 안 됨).
- **변경**: 원본을 사본으로 복사 후 수정. **진짜 원인**: `scan_canvas.py:_draw_sensor_crosshairs`의 `drawPolygon(QPointF,QPointF,QPointF)` — PySide6 미지원 → paintEvent 크래시(첫 jog 이후 상시). `drawPolygon(QPolygonF([...]))`로 수정. 부가: `max_range_m` 5→40, robust auto-fit. 오프스크린 `grab()`로 paintEvent 재현·검증. → [ISSUE-003](../../../issues_and_fixes/003-lidar-view-disappears-on-jog.md)

### 9. 워크스페이스 재구성 (FITO 철학 적용)
- **배경**: FITO_AMR_ros2_ws의 조직 철학 분석 후 우리 ws에 적용 요청.
- **변경**: 목적/생명주기별 최상위 분리 — `amr_lidar/`(패키지) · `apps/lidar_2d_calibration/`(사본 앱, Lidarr_2d_Calibration_Window에서 이동/개명) · `reference/`(읽기전용 벤더: seer-api.pdf, TiM320 datasheet, TiM3xx manual) · `artifacts/` · `docs/`(도메인 분할: protocol/ worklog/ issues_and_fixes/ code_updates/ + docs/README.md 인덱스) · scripts/ tools/ keys/ logs/. `DIRECTORY_GUIDE.txt`(폴더별 설명+철학) + `COMMANDS.txt`(명령 정리) 신규. README를 SSOT 진입점으로 갱신. `python -m amr_lidar` / 앱 compile 재검증.
- **결정**: 단일 패키지라 src/ 층 없이 flat layout 유지(`python -m` 마찰 최소화). ws는 사용자가 `260709_` 접두로 개명.

### 10. 영구 도크 + Find 크래시
- **변경**: broadcast 브리지(`run_lidar_bridge.bat`) 도입 — SEER→MS3를 서브넷(192.168.44.255:6060/6061)으로 broadcast, 어느 머신이든 0.0.0.0:port로 dock. emitter에 `SO_BROADCAST` 추가. 앱 "Find" 크래시(`tools.discover` shadow) 수정 → [ISSUE-005](../../../issues_and_fixes/005-calib-find-tools-discover.md).

### 11. flicker/teleport 완전 수정
- **배경**: 스캔이 튀고 깜빡임(점 수 520↔338/339).
- **변경**: 패킷 추적으로 3중 원인 확정·수정 — MTU초과(프래그먼트화), 중복 에미터(단일 인스턴스+.bat loop 제거), SEER 부분 스캔(드롭). broadcast 8s 전부 520pt 검증. → [ISSUE-004](../../../issues_and_fixes/004-lidar-flicker-teleport.md)

### 12. .bat 실행 문제 + 캘리브레이션 세션
- **변경**: `.bat` 6개 LF→CRLF+ASCII 변환(더블클릭 실행 불가 해결) → [ISSUE-006](../../../issues_and_fixes/006-calib-bat-crlf-launch.md). `cmd /c`로 앱 기동 + 520/520 pts 수신 검증.
- **관찰**: ICP 캘리브레이션 결과 해석 — 벽(선형 feature) 위주 region은 육안 정렬 양호하나 along-wall/회전 제약 약함. "NOT SYMMETRIC"(Δty~7cm)은 실제 장착 비대칭일 수 있어 heuristic 경고로 취급. 미수렴 region은 Min Correspondences(30) 미달(20/23)이 주원인 → 오정렬 아님. 코너(두 벽 교차) region 추가 권장.

## 이슈
- [ISSUE-006](../../../issues_and_fixes/006-calib-bat-crlf-launch.md) — .bat 더블클릭 실행 안 됨 (LF 개행)
- [ISSUE-005](../../../issues_and_fixes/005-calib-find-tools-discover.md) — 앱 Find 크래시 (tools.discover shadow)
- [ISSUE-004](../../../issues_and_fixes/004-lidar-flicker-teleport.md) — flicker/teleport (MTU+중복에미터+부분스캔)
- [ISSUE-001](../../../issues_and_fixes/001-usb-ethernet-subnet-no-data.md) — 2번째 PC에 LiDAR 데이터 미수신 (서브넷/스트림 대상)
- [ISSUE-002](../../../issues_and_fixes/002-udp-relay-wsaeconnreset.md) — Windows UDP 릴레이 control loop 사망 (WSAECONNRESET 10054)
- [ISSUE-003](../../../issues_and_fixes/003-lidar-view-disappears-on-jog.md) — jog 변경 시 스캔이 사라짐 (paintEvent drawPolygon 크래시)

## 비고
- LiDAR 직접 연결에는 **자격증명 불필요** — SEER API는 로봇망에서 개방(19204). `amr/lidar2026`은 내가 만든 UDP 릴레이 전용.
- 남은 선택지(2D safety UDP를 노트북으로 직접): Safety Designer 목적지 지정 / `.5`에 SSH 키 등록 / `.5`에서 뷰어 실행. 현재는 SEER API 경로로 충분.
- 캘리브 앱 GUI 전체는 미구동(오프스크린 canvas 로직만 검증) — 실행 시 육안 확인 권장.
